{
    "name": "syndrasi/syndrasi",
    "description": "SynDrasi - Emergency coordination platform. Dev-only tooling; the app itself has no runtime dependencies.",
    "license": "proprietary",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpstan/phpstan": "^1.11",
        "phpunit/phpunit": "^10.5"
    },
    "config": {
        "sort-packages": true
    }
}
